[build-system]
requires = [
    "setuptools>=42",
    "scikit-build",
    "setuptools",
    "wheel",
    "attrs",
    "packaging>=20.9",
    "editables==0.2",
    "pandas",
    "sklearn",
    "sortedcontainers",
    "gmpy2",
    "matplotlib"
]
build-backend = "setuptools.build_meta"